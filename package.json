{
  "name": "numinia-nwos",
  "private": true,
  "type": "module",
  "description": "The tree and its guards. The web app lives in web/ with its own package.json.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --experimental-test-coverage --test-coverage-include='machine/scripts/**/*.mjs' --test-coverage-include='machine/guards/**/*.mjs' --test-coverage-include='machine/tools/**/*.mjs' --test-coverage-exclude='**/test/**' \"machine/scripts/test/*.test.mjs\" \"machine/guards/test/*.test.mjs\" \"machine/tools/test/*.test.mjs\"",
    "guards": "node machine/scripts/run-guards.mjs"
  }
}
